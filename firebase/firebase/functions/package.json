{
  "name": "functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "lint": "eslint .",
    "serve": "cross-env BACKEND_URL=http://127.0.0.1:8000 BACKEND_SECRET=this-is-my-local-secret-locustsocial firebase emulators:start --only functions,firestore,storage",
    "start": "npm run serve",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "node-fetch": "^2.7.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^3.1.0"
  }
}